<h1>All Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task) %>
      <br>
      <%= task.details%><br>
      Completed : <%= task.completed ? "Yes" : "No" %>
    </li>
  <% end %>
</ul>
