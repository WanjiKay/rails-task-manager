<h1>All Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, tasks_path(task) %>
      <%= task.details%>
      Completed : <%= task.completed ? "Yes" : "No" %>
    </li>
  <% end %>
</ul>
